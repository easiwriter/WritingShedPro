fix: Show warning alert instead of disabling header/footer button when not configured

- Header/footer toolbar button is now always enabled in all file list views
- When tapped without headers/footers enabled in Page Setup, shows an
  informative alert directing the user to enable them in Settings
- Applied to ProseListView, SceneListView, and FolderFilesView
- Added headerFooter.notEnabled.title/message localisation strings
