{
  "name": "wsp-support",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "python3 build-prompt.py",
    "dev": "npm run build && wrangler dev",
    "deploy": "npm run build && wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.93.1"
  }
}
